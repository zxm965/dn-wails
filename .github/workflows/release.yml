name: Build and publish desktop release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_OPTIONS: --max-old-space-size=4096

jobs:
  quality:
    name: Quality checks
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/action-setup@v6.0.9
        with:
          version: 10.33.3

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install frontend dependencies
        run: pnpm --dir frontend install --frozen-lockfile

      - name: Check release tag
        run: node scripts/configure-release.mjs "$GITHUB_REF_NAME"

      - name: Check frontend formatting
        run: pnpm --dir frontend fmt:check

      - name: Lint frontend
        run: pnpm --dir frontend lint

      - name: Build frontend
        run: pnpm --dir frontend build

      - name: Install Linux build dependencies
        run: sudo apt-get update && sudo apt-get install --yes libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Check Go packages
        run: go test -tags webkit2_41 ./...

  build:
    name: Build ${{ matrix.name }}
    needs: quality
    environment: DATABASE
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS universal
            runner: macos-latest
            artifact: release-macos
          - name: Windows amd64
            runner: windows-latest
            artifact: release-windows
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/action-setup@v6.0.9
        with:
          version: 10.33.3

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install frontend dependencies
        run: pnpm --dir frontend install --frozen-lockfile

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.13.0

      - name: Install NSIS
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install nsis --yes --no-progress
          "C:\Program Files (x86)\NSIS" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Configure release version
        run: node scripts/configure-release.mjs "${{ github.ref_name }}"

      - name: Prepare embedded database configuration
        shell: bash
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          node --input-type=module <<'NODE'
          import { writeFile } from 'node:fs/promises'

          const databaseURL = process.env.DATABASE_URL?.trim() ?? ''
          if (!databaseURL) throw new Error('DATABASE_URL is required')
          if (databaseURL.length > 8192) throw new Error('DATABASE_URL cannot exceed 8192 characters')
          if (Array.from(databaseURL).some((character) => {
            const codePoint = character.codePointAt(0)
            return codePoint !== undefined && (
              codePoint <= 0x1f ||
              (codePoint >= 0x7f && codePoint <= 0x9f) ||
              codePoint === 0x2028 ||
              codePoint === 0x2029
            )
          })) {
            throw new Error('DATABASE_URL cannot contain control characters')
          }

          await writeFile('.env.local', `DATABASE_URL=${databaseURL}\n`, { encoding: 'utf8', mode: 0o600 })
          NODE

      - name: Build macOS application
        if: runner.os == 'macOS'
        shell: bash
        run: |
          version="${GITHUB_REF_NAME#v}"
          ldflags="-X dn-wails/internal/buildinfo.Version=$version -X dn-wails/internal/buildinfo.Repository=$GITHUB_REPOSITORY"
          wails build -clean -skipbindings -platform darwin/universal -o dn-wails -trimpath -ldflags "$ldflags"
          codesign --force --deep --sign - build/bin/dn-wails.app
          ditto -c -k --sequesterRsrc --keepParent build/bin/dn-wails.app build/bin/dn-wails-darwin-universal.zip
          hdiutil create -volname "dn-wails" -srcfolder build/bin/dn-wails.app -ov -format UDZO build/bin/dn-wails-darwin-universal.dmg

      - name: Build Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME.TrimStart('v')
          $ldflags = "-X dn-wails/internal/buildinfo.Version=$version -X dn-wails/internal/buildinfo.Repository=$env:GITHUB_REPOSITORY"
          wails build -clean -skipbindings -platform windows/amd64 -o dn-wails.exe -nsis -installscope user -trimpath -ldflags $ldflags
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          Move-Item build/bin/dn-wails-amd64-installer.exe build/bin/dn-wails-windows-amd64-installer.exe

      - name: Upload macOS packages
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: |
            build/bin/dn-wails-darwin-universal.zip
            build/bin/dn-wails-darwin-universal.dmg
          if-no-files-found: error

      - name: Upload Windows package
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: build/bin/dn-wails-windows-amd64-installer.exe
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Download packages
        uses: actions/download-artifact@v8
        with:
          path: release
          merge-multiple: true

      - name: Generate checksums
        shell: bash
        run: sha256sum release/* > release/SHA256SUMS.txt

      - name: Create or update release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" release/* --clobber
          else
            gh release create "$GITHUB_REF_NAME" release/* --verify-tag --generate-notes --title "$GITHUB_REF_NAME"
          fi
